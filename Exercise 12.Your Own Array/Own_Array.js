var own_array = ["I would like to own a honda civic.",
    "Some times I like to travel in the train.",
    "The motorway is not bad idea if we have own car.",
    "I did not travel on a cruise ship but I would like to buy a ticket to travel on it.",
];
for (var _i = 0, own_array_1 = own_array; _i < own_array_1.length; _i++) {
    var own_array_value = own_array_1[_i];
    console.log("\"".concat(own_array_value, "\""));
}
